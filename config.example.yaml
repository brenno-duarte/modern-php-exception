title: 
dark_mode: false

# Enable `production mode` in Modern PHP Exception
production_mode: false
error_message: 

# Use `false` only if you have no internet connection
enable_cdn_assets: true

# Enable logs register
enable_logs: false
dir_logs: 

# Database for Occurrences
db_drive: mysql
db_host: localhost
db_name: test
db_user: root
db_pass: 